# Исправления ошибок Smart Log

**Дата начала**: 2026-04-15

---

## ✅ Выполнено

### 1. Yandex Smart Home: deprecated device type fan
- **Файл**: `Z:\includes\System\system_yandex_smart_home.yaml`
- **Статус**: Уже исправлено (используется `devices.types.ventilation.fan`)
- Ошибка была от старой версии конфигурации до перезагрузки

### 2. Automation: System_Notify_HA_Restart — сообщение слишком длинное
- **Файлы**:
  - `Z:\includes\System\system_ha_restart_notify.yaml`
  - `Z:\System\system_ha_restart_notify.yaml`
- **Исправление**: Список устройств ограничен до 15 элементов. Если больше — показывается `... и ещё N устройств`
- Конфигурация перезагружена (`reload_core_config`)

---

## ⏳ Осталось исправить

### 3. Telegram Bot: Conflict (20 ошибок)
- **Причина**: Два экземпляра бота пытаются использовать polling одновременно
- **Что сделать**:
  - Проверить интеграцию Telegram Bot в UI (Configuration → Integrations)
  - Убедиться, что бот не запущен в другом месте (другой HA, скрипт, бэкап)
  - Если интеграция не нужна — удалить её

### 4. MQTT: Erroneous JSON (10 ошибок)
- **Источник**: `homeassistant/components/mqtt/entity.py` строка 559
- **Что сделать**:
  - Получить детальные логи через API
  - Найти устройство/сенсор, отправляющий некорректный JSON
  - Исправить конфигурацию устройства или MQTT-сенсора

### 5. Template: switch_type_button отсутствует в JSON (4 ошибки)
- **Причина**: Шаблон использует `value_json.switch_type_button`, но устройство не отправляет это поле
- **Что сделать**:
  - Найти MQTT-сенсор (настроен через UI, в файлах не найден)
  - Добавить проверку на наличие поля в шаблоне или удалить неиспользуемый атрибут

### 6. ESPHome: 192.168.10.77 недоступно (1 ошибка)
- **Устройство**: `tests @ 192.168.10.77`
- **Что сделать**:
  - Проверить, существует ли устройство
  - Если удалено/выключено — удалить из конфигурации ESPHome

---

## Заметки

- Автоматизация `System_Notify_HA_Restart` также имела `parse_mode: markdown` в одном файле и `parse_mode: html` в другом — привести к единому формату
- Проверить, какой файл подключается в `configuration.yaml` (`includes` или `System`)
